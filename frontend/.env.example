# Public API base URL for the browser (no trailing slash). Required in production.
NEXT_PUBLIC_API_BASE=

# Optional local-dev login prefill only — never set in production
NEXT_PUBLIC_DEV_USERNAME=
NEXT_PUBLIC_DEV_PASSWORD=
